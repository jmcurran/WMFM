% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.wmfmRepeatedExplanationRuns.R,
%   R/plotWmfmExplanationClaimHeatmap.R
\name{plotWmfmExplanationClaimHeatmap}
\alias{plotWmfmExplanationClaimHeatmap}
\title{Plot a heatmap of semantic explanation claims across repeated WMFM runs}
\usage{
plotWmfmExplanationClaimHeatmap(
  runsDf,
  claimColumns = NULL,
  runIdColumn = "runId",
  sortRows = TRUE,
  naLabel = "(missing)",
  main = "WMFM explanation claim heatmap",
  xlab = "",
  ylab = "Run",
  cexAxis = 0.8,
  cexLegend = 0.85,
  xLabelSrt = 30,
  mar = c(10, 8, 4, 2),
  legendWidth = 2.8
)

plotWmfmExplanationClaimHeatmap(
  runsDf,
  claimColumns = NULL,
  runIdColumn = "runId",
  sortRows = TRUE,
  naLabel = "(missing)",
  main = "WMFM explanation claim heatmap",
  xlab = "",
  ylab = "Run",
  cexAxis = 0.8,
  cexLegend = 0.85,
  xLabelSrt = 30,
  mar = c(10, 8, 4, 2),
  legendWidth = 2.8
)
}
\arguments{
\item{runsDf}{A data.frame of run records, or a list containing a
data.frame named \code{runsDf}.}

\item{claimColumns}{Optional character vector giving the columns to plot.}

\item{runIdColumn}{Character. Column to use for row labels.}

\item{sortRows}{Logical. Should rows be sorted by claim pattern?}

\item{naLabel}{Character. Label used for missing values.}

\item{main}{Character. Plot title.}

\item{xlab}{Character. X-axis label. Defaults to \code{""}.}

\item{ylab}{Character. Y-axis label.}

\item{cexAxis}{Numeric. Axis text expansion factor.}

\item{cexLegend}{Numeric. Legend text expansion factor.}

\item{xLabelSrt}{Numeric. Rotation angle in degrees for x-axis tick labels.}

\item{mar}{Numeric vector of length 4 giving heatmap plot margins.}

\item{legendWidth}{Numeric. Relative width allocated to the legend panel.}

\item{legendRightInset}{Numeric. Right-side inset used for the legend.}
}
\value{
Invisibly returns a list describing the plotted data.

Invisibly returns a list describing the plotted data.
}
\description{
Draws a run-by-claim heatmap to help assess whether repeated model
explanations differ in substantive meaning rather than only word choice.

Draws a run-by-claim heatmap to help assess whether repeated model
explanations differ in substantive meaning rather than only word choice.
}
\details{
Rows represent runs and columns represent extracted semantic claim
variables. Each unique value within a claim column is assigned a colour.

The input may be either:
\itemize{
\item a data.frame of run records, or
\item a list returned by \code{runWMFMPackageExampleRepeated()} containing a
\code{runsDf} element.
}

If \code{claimColumns = NULL}, the function uses a default set of semantic claim
columns when present.

Colour assignment is deterministic and uses HCL-based colours so that:
\itemize{
\item categories are more visually distinct,
\item the mapping is stable across plots, and
\item larger numbers of unique values are handled better.
}

Rows represent runs and columns represent extracted semantic claim
variables. Each unique value within a claim column is assigned a colour.

The input may be either:
\itemize{
\item a data.frame of run records, or
\item a list returned by \code{runWMFMPackageExampleRepeated()} containing a
\code{runsDf} element.
}

If \code{claimColumns = NULL}, the function uses a default set of semantic claim
columns when present.

Colour assignment uses the named WMFM semantic colour map first. Any unseen
values are assigned colours deterministically from a fallback qualitative
palette so that the plot remains stable and readable.
}
\examples{
\dontrun{
plotWmfmExplanationClaimHeatmap(repeatedRuns)
}
}
