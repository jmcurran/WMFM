% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/describeWmfmField.R
\name{describeWmfmField}
\alias{describeWmfmField}
\title{Describe a WMFM field}
\usage{
describeWmfmField(
  field,
  format = c("text", "list", "data.frame"),
  includeExamples = TRUE,
  includeAliases = TRUE
)
}
\arguments{
\item{field}{Character scalar naming the field to describe. This may be a
canonical field name, a recognised alias, or one of the pretty labels shown
on the heatmap x-axis.}

\item{format}{Character. One of \code{"text"}, \code{"list"}, or \code{"data.frame"}.
The default \code{"text"} prints a formatted character vector with \code{cat()} and
returns that vector invisibly. \code{"list"} returns the underlying structured
description. \code{"data.frame"} returns a one-row data frame.}

\item{includeExamples}{Logical. Should examples be included in the returned
output?}

\item{includeAliases}{Logical. Should recognised aliases be included in the
returned output?}
}
\value{
If \code{format = "text"}, a formatted character vector returned
invisibly after being printed to the console. Otherwise a named list or a
one-row data frame describing the requested field.
}
\description{
Prints a human-readable description of a field used in the WMFM workflow.
This is intended to help interpret the raw run fields, judged fields, and
aggregate score fields that appear in WMFM objects and related plots.
}
\details{
The function accepts:
\itemize{
\item current schema names such as \code{interactionEvidenceAppropriate},
\item older aliases such as \code{interactionInference}, and
\item the pretty x-axis labels used by \code{plotWmfmExplanationClaimHeatmap()},
such as \code{"Direction claim"}, \code{"Scale claim"}, or \code{"CI mention"}.
}

By default the function prints a formatted explanation to the console using
\code{cat()} and returns the underlying character vector invisibly. This makes it
convenient for interactive use while still allowing the text to be captured
programmatically when needed.

For judged fields scored on a \code{0}/\code{1}/\code{2} scale, the usual interpretation is:
\describe{
\item{0}{Poor, missing, inappropriate, or clearly problematic.}
\item{1}{Partly adequate, weak, borderline, or unclear.}
\item{2}{Appropriate, clear, or well handled.}
}
}
\examples{
describeWmfmField("interactionEvidenceAppropriate")
describeWmfmField("Direction claim")
describeWmfmField("CI mention")
describeWmfmField("overallScore", format = "list")
}
