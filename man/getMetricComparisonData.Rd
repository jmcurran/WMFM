% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scoring-comparison-data.R
\name{getMetricComparisonData}
\alias{getMetricComparisonData}
\title{Extract run-level comparison data for a metric}
\usage{
getMetricComparisonData(x, metric)
}
\arguments{
\item{x}{A \code{wmfmScoreComparison} object.}

\item{metric}{Character name of the metric. Must be one of the metric names
stored in \code{x$registry$metricName}.}
}
\value{
An object of class \code{metricComparisonData} with one row per run.
When one of the compared methods is deterministic, the returned data frame
includes a \code{detValue} column and a method-specific value column such as
\code{llmValue}. When the comparison is between deterministic and LLM scoring,
the output also includes \code{llmMinusDet} and \code{detMinusLlm}.
}
\description{
Returns run-level paired scores for a given metric from a
\code{wmfmScoreComparison} object.
}
\details{
This accessor uses the metric registry stored on the comparison object as the
source of truth for valid metric names. Because the choices are dynamic,
\code{match.arg()} is used for validation and partial matching, but IDE tab
completion will not show the possible values automatically.
}
