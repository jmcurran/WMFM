% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runWMFMExamples.R
\name{runWMFMPackageExample}
\alias{runWMFMPackageExample}
\title{Run a packaged WMFM example}
\usage{
runWMFMPackageExample(name, package = "WMFM", printOutput = TRUE, ...)
}
\arguments{
\item{name}{A character string giving the name of the example folder under
\code{inst/extdata/examples}. The specification file is assumed to be named
\verb{<name>.spec.yml}.}

\item{package}{A character string giving the package name containing the
packaged examples.}

\item{printOutput}{Logical. Passed to \code{runModel()}. If \code{TRUE},
prints the model summary, fitted equations, and explanation.}

\item{...}{Additional arguments passed on to \code{runModel()}.}
}
\value{
Invisibly returns the result of \code{runModel()}.
}
\description{
Loads a stored example from the package's \code{inst/extdata/examples}
directory, reads its specification, data, and optional context, and then
runs \code{runModel()} using those inputs.
}
\details{
Each example is expected to live in its own subdirectory under
\verb{extdata/examples/<name>/} and to contain a specification file named
\verb{<name>.spec.yml}.

Supported data sources are:
\describe{
\item{\code{file}}{A data file bundled with the example directory. Supported
formats are \code{.csv}, \code{.txt}, \code{.rda}, and \code{.RData}.}
\item{\code{package}}{A dataset loaded from another installed package, such as
\code{s20x}.}
}

For package-based data sources, if no explicit context file is supplied in
the specification, the function attempts to use the associated package help
page as the data context.
}
\examples{
\dontrun{
runWMFMPackageExample("Course", package = "WMFM")
}

}
