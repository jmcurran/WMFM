% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auditBadExplanationGrading.R
\name{auditBadExplanationGrading}
\alias{auditBadExplanationGrading}
\title{Audit whether bad explanations are being penalised by the current rubric}
\usage{
auditBadExplanationGrading(
  x,
  goodGrade = NULL,
  method = c("deterministic", "llm"),
  minExpectedDrop = 1,
  flaggedThreshold = 9,
  expectedMetrics = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{wmfmGradeListObj} containing graded bad explanations.}

\item{goodGrade}{Optional \code{wmfmGrade} object for a reference good
explanation.}

\item{method}{Character scalar. Grading method to audit. One of
\code{"deterministic"} or \code{"llm"}.}

\item{minExpectedDrop}{Numeric scalar. Minimum drop in mark, relative to the
good explanation, required before an explanation is treated as clearly
penalised.}

\item{flaggedThreshold}{Numeric scalar. Any explanation with a mark greater
than or equal to this threshold is flagged.}

\item{expectedMetrics}{Optional named list. Each element name should match an
explanation name in \code{x}, and each element value should be a character
vector of metric names that you expected to lose marks.}

\item{...}{Unused.}
}
\value{
An object of class \code{wmfmBadExplanationAudit}.
}
\description{
Builds a compact audit summary around a set of already graded bad
explanations, with optional comparison against a graded good explanation.
This is intended as a lightweight helper for diagnosing adversarial or
deliberately flawed explanations that may be slipping through the current
deterministic rubric.
}
\examples{
\dontrun{
badGrades = grade(modelObj, explanation = badVec, method = "deterministic")
goodGrade = grade(modelObj, explanation = modelObj$explanation, method = "deterministic")

audit = auditBadExplanationGrading(
  badGrades,
  goodGrade = goodGrade,
  expectedMetrics = list(
    effectDirectionError = c("factualScore"),
    wrongScaleError = c("factualScore", "clarityScore"),
    rSquaredOverclaim = c("factualScore", "calibrationScore"),
    logicalContradiction = c("factualScore", "clarityScore")
  )
)

print(audit)
}
}
