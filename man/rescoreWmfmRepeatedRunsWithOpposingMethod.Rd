% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rescoreWmfmRepeatedRunsWithOpposingMethod.R
\name{rescoreWmfmRepeatedRunsWithOpposingMethod}
\alias{rescoreWmfmRepeatedRunsWithOpposingMethod}
\title{Rescore repeated WMFM runs with the opposing method}
\usage{
rescoreWmfmRepeatedRunsWithOpposingMethod(
  x,
  scoringChat = NULL,
  useScoringCache = FALSE,
  verbose = FALSE,
  showProgress = TRUE
)
}
\arguments{
\item{x}{Object produced by \code{runWMFMPackageExampleRepeated()}.}

\item{scoringChat}{Optional chat provider object used when rescoring with the
LLM.}

\item{useScoringCache}{Logical. Passed to \code{scoreWmfmRunWithLlm()} when the
opposing method is \code{"llm"}.}

\item{verbose}{Logical. Passed to \code{scoreWmfmRunWithLlm()} when the opposing
method is \code{"llm"}.}

\item{showProgress}{Logical. Should a console progress bar be shown?}
}
\value{
The input object with additional prefixed columns in \code{runsDf} and an
\code{opposingScoringMethod} element.
}
\description{
Takes an existing repeated-run object and scores each run again with the
opposite of its primary scoring method. The newly computed scores are stored
alongside the existing scores using either a \code{"det_"} or \code{"llm_"} prefix.
}
