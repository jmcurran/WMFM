% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/computeMeanCi.R
\name{computeMeanCi}
\alias{computeMeanCi}
\title{Compute confidence intervals for fitted mean responses}
\usage{
computeMeanCi(
  model,
  newData,
  ciType = "standard",
  hcType = "HC0",
  level = 0.95
)
}
\arguments{
\item{model}{A fitted \code{lm} or \code{glm} object.}

\item{newData}{A data frame of covariate values at which to evaluate the
fitted mean.}

\item{ciType}{Character string: \code{"standard"} or \code{"sandwich"}.}

\item{hcType}{Character string specifying the HC estimator
(e.g. \code{"HC0"}, \code{"HC3"}). Used only when
\code{ciType = "sandwich"}.}

\item{level}{Confidence level for the intervals.}
}
\value{
A data frame containing \code{newData} plus columns
\code{fit}, \code{lower}, and \code{upper}.
}
\description{
Computes pointwise confidence intervals for the fitted mean response
evaluated at \code{newData}. These are confidence intervals for the
conditional mean (i.e., the fitted line), not prediction intervals.
}
\details{
For generalized linear models, intervals are computed on the link scale
and transformed back to the response scale.

Robust (sandwich) confidence intervals are supported via \code{vcovHC()}.
}
