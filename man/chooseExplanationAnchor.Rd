% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-explanationAnchor.R
\name{chooseExplanationAnchor}
\alias{chooseExplanationAnchor}
\title{Choose deterministic explanation anchors for numeric predictors}
\usage{
chooseExplanationAnchor(
  model = NULL,
  mf = NULL,
  predictorNames = NULL,
  userAnchors = NULL,
  meaningfulAnchors = NULL,
  defaultSource = c("median", "mean")
)
}
\arguments{
\item{model}{Optional fitted model object. Used only when \code{mf} is not
supplied.}

\item{mf}{Optional model frame. If omitted, it is computed from \code{model}.}

\item{predictorNames}{Optional character vector of predictor names. If
omitted, all non-response columns in \code{mf} are considered.}

\item{userAnchors}{Optional named numeric vector or list of user-specified
anchors. These take priority over all other anchor sources.}

\item{meaningfulAnchors}{Optional named numeric vector or list of
pedagogically meaningful anchors. These are used after \code{userAnchors}.}

\item{defaultSource}{Character scalar. Either \code{"median"} or \code{"mean"}.}
}
\value{
A named list containing anchor metadata, prompt text, and a cache key.
}
\description{
Builds auditable anchor metadata for student-facing explanations. These
anchors are intended for explanation and prompt inputs, not for changing the
fitted model. The raw anchor value is kept separately from the displayed
value so explanations can use readable numbers while calculations remain
traceable.
}
\keyword{internal}
