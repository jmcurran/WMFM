% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getFactorPredictors.R
\name{getFactorPredictors}
\alias{getFactorPredictors}
\title{Extract factor predictors from a fitted model}
\usage{
getFactorPredictors(model, data)
}
\arguments{
\item{model}{A fitted model object (e.g. \code{lm}, \code{glm}).}

\item{data}{A data frame containing the variables used to fit the model.}
}
\value{
A character vector of factor predictor names. May be empty.
}
\description{
Returns the names of predictor variables in a fitted model that are
factors in the supplied data set.
}
\examples{
df = data.frame(
  y = rpois(10, 3),
  site = factor(rep(c("A", "B"), each = 5)),
  depth = rnorm(10)
)
mod = glm(y ~ site + depth, family = poisson, data = df)
getFactorPredictors(mod, df)

}
