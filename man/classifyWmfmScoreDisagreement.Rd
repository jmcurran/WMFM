% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classifyWmfmScoreDisagreement.R
\name{classifyWmfmScoreDisagreement}
\alias{classifyWmfmScoreDisagreement}
\title{Classify run-level score disagreement for heatmap plotting}
\usage{
classifyWmfmScoreDisagreement(
  x,
  includeContinuous = FALSE,
  continuousBreaks = c(0.1, 0.35, 0.75)
)
}
\arguments{
\item{x}{A \code{wmfmScoreComparison} object.}

\item{includeContinuous}{Logical. Should continuous metrics be classified?}

\item{continuousBreaks}{Numeric vector of length 3 giving cut points for the
absolute difference bins used for continuous metrics.}
}
\value{
A data frame like \code{x$pairData} with an added
\code{disagreementClass} column.
}
\description{
Converts paired run-level values from \code{buildWmfmComparisonPairs()} into a
categorical disagreement label that can be used for a heatmap.
}
\details{
Classification rules are:
\itemize{
\item binary metrics: \code{"exact"} or \code{"different"};
\item ordinal metrics: \code{"exact"}, \code{"adjacent"}, \code{"moderate"}, or \code{"large"};
\item continuous metrics: excluded by default in the heatmap and returned
as \code{NA} unless \code{includeContinuous = TRUE}, in which case they are binned as
\code{"exact"}, \code{"small"}, \code{"moderate"}, or \code{"large"} using absolute
differences.
}

For ordinal metrics, ordered levels are taken from the registry stored on the
comparison object.
}
\keyword{internal}
