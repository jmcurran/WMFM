% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runWMFMPackageExampleRepeated.R
\name{runWMFMPackageExampleRepeated}
\alias{runWMFMPackageExampleRepeated}
\title{Run a packaged WMFM example multiple times and collate outputs}
\usage{
runWMFMPackageExampleRepeated(
  name,
  package = "WMFM",
  nRuns = 10,
  printOutput = FALSE,
  pauseSeconds = 0,
  showProgress = TRUE,
  useExplanationCache = FALSE,
  interactionAlpha = 0.05,
  scoringMethod = c("deterministic", "llm", "none"),
  scoringChat = NULL,
  useScoringCache = FALSE,
  verboseScoring = FALSE,
  ...
)
}
\arguments{
\item{name}{Character. Name of the example folder.}

\item{package}{Character. Package name.}

\item{nRuns}{Integer. Number of repetitions.}

\item{printOutput}{Logical. Passed to \code{runWMFMModelDebug()}.}

\item{pauseSeconds}{Numeric. Optional delay between runs.}

\item{showProgress}{Logical. Should a console progress bar and estimated
time remaining be shown?}

\item{useExplanationCache}{Logical. Passed to \code{runWMFMModelDebug()}.
Defaults to \code{FALSE} so repeated runs query the language model each time.}

\item{interactionAlpha}{Numeric. Threshold used when judging whether an
explanation interpreted interaction evidence appropriately.}

\item{scoringMethod}{Character. One of \code{"deterministic"}, \code{"llm"}, or
\code{"none"}.}

\item{scoringChat}{Optional chat provider object used when
\code{scoringMethod = "llm"}.}

\item{useScoringCache}{Logical. Passed to \code{scoreWmfmRunWithLlm()} when
\code{scoringMethod = "llm"}.}

\item{verboseScoring}{Logical. Passed to \code{scoreWmfmRunWithLlm()} when
\code{scoringMethod = "llm"}.}

\item{...}{Additional arguments passed to \code{runWMFMModelDebug()}.}
}
\value{
A list with elements:
\describe{
\item{runsDf}{Data frame of all runs}
\item{summary}{Summary statistics}
\item{spec}{Specification}
\item{dataContext}{Context text}
\item{primaryScoringMethod}{Scoring method used during the initial run}
}
}
\description{
Loads a stored example from the package's \code{inst/extdata/examples}
directory, reads its specification, data, and optional context, and then
runs \code{runWMFMModelDebug()} repeatedly using those same inputs.
}
\details{
This is useful for checking response stability across repeated calls to the
language model for the same fitted model. The function collects the raw
equations and explanation outputs from each run and computes simple text
features and semantic claim fields that help compare the explanations.

A console progress bar is displayed by default. After each run, the function
updates the estimated time remaining using the average elapsed time per
completed run.

By default, explanation caching is disabled in repeated-run mode so that
each repetition makes a fresh explanation request to the language model.
}
