% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/isFactorOnlyModel.R
\name{isFactorOnlyModel}
\alias{isFactorOnlyModel}
\title{Check whether a fitted model has only factor predictors}
\usage{
isFactorOnlyModel(model, data)
}
\arguments{
\item{model}{A fitted model object (e.g. \code{lm}, \code{glm}).}

\item{data}{A data frame containing the variables used to fit the model.}
}
\value{
Logical scalar. Returns \code{TRUE} if all predictors are factors,
otherwise \code{FALSE}.
}
\description{
Determines whether all predictors in a fitted linear or generalised
linear model are factors in the supplied data set.
}
\details{
Intercept-only models are not considered factor-only.
}
\examples{
df = data.frame(
  y = rpois(20, 5),
  site = factor(rep(c("A", "B"), each = 10))
)
mod = glm(y ~ site, family = poisson, data = df)
isFactorOnlyModel(mod, df)

}
