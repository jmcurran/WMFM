# Stage 17.12.1 server inventory

Stage 17.12.1 fixes tests after the structural startup observer extraction.

## Test-only updates

- Add local package-source text helpers to `test-app-server-startup-observers.R`.
- Read `R/app-server-startup.R` in startup loading UI tests.
- Update developer-mode UI tests to search the combined app server and startup observer source text.

## Behaviour

No app behaviour is changed in this patch.
