# Stage 45.8.2: permanent-name repair

This repair removes stage numbers from permanent developer example and test names.

The observation-question fixtures now use descriptive directory names, specification filenames, display names and metadata keys. The associated test file and test descriptions are also descriptive. The stage-numbered files introduced during Stage 45.8 are removed by the stage runner before the replacements are installed.

No production classification, residual computation, comparable-observation computation or user-facing classroom behaviour changes in this repair.
