# Stage 49.8.11: specialised output assembly

This repair prevents the final `runModel()` post-processing step from appending an anchored generic factor comparison after a complete research-question response. It also makes comparison evaluation use the successful comparison payload, ensures clarification text lists every missing fitted-model predictor, refers directly to explicit thresholds, and improves deterministic comparison wording.
